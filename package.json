{
  "name": "sns-post-dashboard-pages",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:dashboard": "node scripts/generate-dashboard.mjs",
    "build": "node scripts/encrypt-page.mjs --input '../SNS横断ダッシュボード_X_Threads1ヶ月実データ版.html' --output docs/index.html --password-stdin",
    "qa:built": "node scripts/qa-built-page.mjs",
    "test": "node --test --experimental-test-coverage"
  },
  "devDependencies": {
    "puppeteer": "25.9.0"
  }
}
